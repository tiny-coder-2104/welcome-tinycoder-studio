# Smoke test run log — 2026-09-24

Plan: `tests/plans/smoke.md` | Executor: tester | Target: https://welcome-tinycoder-studio.vercel.app (prod)
Result: **8 PASS / 0 FAIL / 4 BLOCKED** (browser checks blocked by infra, not app)

## 1. Local — 6 demo suites
| Check | Result | Evidence |
|-------|--------|----------|
| 1. run-all.sh | **PASS** | exit 0; `ALL SUITES GREEN`; 6/6: app.js, dashboard/rank.js, api/chat.js, api/leads.js, lib/suggest.js, api/suggest.js |

## 2. Live API E2E (curl, deployed)
| Check | Result | Evidence |
|-------|--------|----------|
| 2. Static pages 200 | **PASS** | index.html:200 intake.html:200 contact.html:200 what-we-do.html:200 pricing-faq.html:200 |
| 3. Secrets grep | **PASS** | grep -cE 'SUPABASE_SERVICE_ROLE_KEY\|NVIDIA_API_KEY\|AGENTMAIL_API_KEY' over 7 client-served URLs → 0 |
| 4. RLS anon | **PASS** | `GET /rest/v1/leads?select=id&limit=1` anon → HTTP 200, body `[]` (deny-by-absence) |
| 5. Lead capture (intake) | **PASS** | POST /api/chat lead → HTTP 200, `{"text":"Got it. We'll reply within 1 business day.","leadId":"40e2789c-2976-4990-8f79-fa8102a8ca88"}`; row verify (service role): `source=CONCIERGE, stage=NEW, priority=MEDIUM, problem_tags=["booking"], type="Web Applications"` |
| 6. Contact branch | **PASS** | POST /api/chat contact → HTTP 200, leadId `baf833a5-46fe-4aae-b91a-417e0b0f070b`; row verify: `type="General Inquiry"`, `problem="[contact] Do you build WhatsApp bots for small resorts? Need pricing info please."` |
| 7. Rate limiter | **PASS** (note) | 11× POST /api/chat `{}` → 8× 400, 3× 429. 429 body = `"Whoa there! Take a breath — I'm here to help with TinyCoder services..."`. Expected 10×400+1×429; burst split across serverless instances → 429 demonstrably fires, exact split instance-dependent. |
| 8. Unauth /api/leads | **PASS** | POST /api/leads `{"name":"x"}` → HTTP 401, body `{"error":"unauthorized"}` (exact match) |

## 3. Browser (navigator, prod) — BLOCKED
| Check | Result | Evidence |
|-------|--------|----------|
| 9. Landing 5 pills | **BLOCKED** | navigator delegation failed: `Subagent depth limit reached (1). Increase "subagent_depth" to allow nested subagents.` — tester is already a subagent; spawning navigator exceeds depth. Infra/config issue, not app. |
| 10. Chat hidden by default | **BLOCKED** | same error |
| 11. Hero no overlap | **BLOCKED** | same error |
| 12. Dashboard login wall | **BLOCKED** | same error |

Fix: raise `subagent_depth` (opencode config) or have QA/oracle invoke navigator directly (top-level). No screenshots produced — `logs/navigator/2026-09-24-smoke/` not created.

## Notes / surprises
- Secrets env var names are `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` (not `ANON_KEY`/`SERVICE_ROLE_KEY` as in plan §2.3) — sourced correctly.
- Rate limiter works but 429 distribution is nondeterministic on serverless (8/3 split vs 10/1) — plan's anticipated risk, recorded actual.
- Test leads created (fake PII only): `smoke-1790205524@example.com` (intake), `smoke-1790205539@example.com` (contact). No prod rows mutated.
- No bug tickets filed — no app failures observed. Browser checks need re-run once depth limit is lifted.
